export interface NewsModel {
    news?: any ;
}
export interface News {
    date?: string;
    newsTitle?: String;
    newsDescription?: String;
    newsAction?: boolean;
    title?: string;
    description?: string;
}

export interface News {
    news: News;
}
